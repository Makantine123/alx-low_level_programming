#include "mainh"
/**
 * wildcmp - Function compares strings and returns 1
 * if the strings can be considered identical, otherwise
 * return 0.
 * @s1: String 1
 * @s2: String 2 can contain special character *
 * Return: 1 if identical otherwise return 0
 */
int wildcmp(char *s1, char *s2)
{
	int i, int j;
}
