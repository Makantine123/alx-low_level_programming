#include "main.h"
/**
 * print_alphabet_x10 - Prints letter in small case 10x and then new line
 * Return: Always 0 (Success)
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (0);
}
else
{
return (1);
}
}
